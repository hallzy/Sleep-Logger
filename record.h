#ifndef __RECORD_H_INCLUDED__
#define __RECORD_H_INCLUDED__

#include "time.h"
#include "fileOutput.h"

// Records the new times
void Record(StartTime, DeltaTime, WakeTime, toFile);

#endif

